I'm new to this session and need project context before doing anything else. Please:

1. **Read `AGENTS.html`** at the repo root. It is the maintained onboarding doc for natKit: project summary, repo map, architecture, channel/stream model, schema conventions, frontend/backend structure, example projects (natVR, natKit-IMU, natMuse), core workflows, key entrypoints, and known constraints.

2. **Read `.claude/state.md`** for current in-progress work, and skim `.claude/decisions.md` for standing architectural decisions.

3. **Skim `ADR/`** if it exists and the task at hand looks architecture-sensitive.

4. Briefly confirm your understanding of what natKit is and where the relevant code likely lives for the task at hand, then ask what I'd like to work on (unless I've already stated it).

Do not ask clarifying questions about this step itself — just read the files and summarize.
