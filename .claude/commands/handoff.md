Before I close this session, perform the following handoff procedure:

1. **Update `.claude/state.md`** with:
   - Current timestamp
   - What was accomplished this session
   - What's in progress but incomplete
   - Clear next steps for resuming
   - Any context that would be lost (error messages, discoveries, etc.)
   - List of files modified

2. **Store significant items in graphiti-memory** (if any):
   - Architectural decisions made → `add_episode` with rationale
   - Approaches that failed → `add_episode` with what went wrong
   - New understanding of the codebase → `add_episode` as reference

3. **Summarize** the handoff state so I can see what you've preserved.

Do NOT ask clarifying questions - make reasonable judgments about what's worth preserving.
